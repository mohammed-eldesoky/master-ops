

export enum Gender {
    MALE = 'male',
    FEMALE = 'female'
}
export enum USER_AGENT {
    local = 'local',
    google = 'google',
    facebook = 'facebook',
  
}

export enum TOKEN_TYPE {
    REFRESH = 'refresh',
    ACCESS = 'access',
}